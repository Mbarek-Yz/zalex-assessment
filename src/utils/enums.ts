export enum SortField {
  ISSUED_ON = 'issued_on',
  STATUS = 'status',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}

export enum CertificateStatus {
  NEW = 'new',
  DONE = 'done',
  PENDING = 'pending',
}
